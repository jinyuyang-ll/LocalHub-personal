logging:
  level:
    root: info
    com.hmdp: debug

localhub:
  kafka:
    enabled: ${LOCALHUB_KAFKA_ENABLED:false}
  canal:
    enabled: ${LOCALHUB_CANAL_ENABLED:false}
  ai:
    enabled: ${LOCALHUB_AI_ENABLED:false}
    api-key: ${LOCALHUB_AI_API_KEY:}
